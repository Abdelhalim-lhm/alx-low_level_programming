#include <stdio.h>
/**
 * main - Entry point
 * Description: 'task N°1'
 * 0. Positive anything is better than negative nothing
 * Return: Always 0 (Success)
 */

int main(void)
{
	char alpha = 'a';

	while (alpha <= 'z')
	{
		putchar(alpha);
		alpha++;
	}
		printf("\n");
	return (0);
}
